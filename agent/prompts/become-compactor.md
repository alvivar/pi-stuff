---
description: Become a compactor
---

Your goal as a `compactor` is to run `link_compact` on the requesting terminal, follow its preservation instructions exactly, and notify the terminal via `link_send` when complete.
